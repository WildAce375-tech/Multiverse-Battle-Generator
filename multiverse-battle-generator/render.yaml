services:
  - type: web
    name: multiverse-battle-generator
    runtime: node
    buildCommand: echo "No build step"
    startCommand: npm start
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: OPENAI_MODEL
        value: gpt-5.6
